export type Campaign = { id: string; name: string; senderId: string; contactListId: string; selectedTemplateId: string; objective: string; audience: string; status: 'DRAFT' | 'READY' | 'RUNNING' | 'PAUSED' | 'STOPPED' | 'COMPLETED' | 'SCHEDULED'; safetyStatus: 'pending' | 'ready' | 'blocked'; schedule: { startAt: string; timezone: string; days: number[]; windowStart: string; windowEnd: string; dailyLimit: number }; lastSafetyCheck?: { checks?: Array<{ label: string; passed: boolean }> } };
