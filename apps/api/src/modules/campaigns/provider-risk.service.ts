export type ProviderRisk = 'auth' | 'temporary' | 'restriction' | 'hard_bounce' | 'unknown';
export function classifyProviderError(error: unknown): ProviderRisk { const candidate = error as { code?: number | string; response?: { status?: number; data?: { error?: { message?: string } } }; message?: string }; const status = Number(candidate.code ?? candidate.response?.status ?? 0); const message = `${candidate.message ?? ''} ${candidate.response?.data?.error?.message ?? ''}`.toLowerCase(); if (status === 401 || /invalid_grant|invalid credentials|unauthenticated|token.*expired/.test(message)) return 'auth'; if (/hard bounce|mailbox.*not found|user unknown|recipient address rejected|invalid recipient|address.*does not exist/.test(message)) return 'hard_bounce'; if (status === 429 || status >= 500 || /rate limit|quota|temporar|backend error/.test(message)) return 'temporary'; if (status === 403 || /restricted|suspended|not authorized|provider restriction/.test(message)) return 'restriction'; return 'unknown'; }
